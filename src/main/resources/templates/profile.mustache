{{>layouts/header}}
<div class="container py-5">
    <div class="card">
        <div class="card-body">
            <h2 class="card-title mb-4">프로필</h2>
            <div class="mb-3">
                <strong>이름:</strong> {{user.name}}
            </div>
            <div class="mb-3">
                <strong>나이:</strong> {{user.age}}세
            </div>
            <div class="mb-3">
                <strong>성별:</strong> {{user.gender}}
            </div>
            <div class="mb-3">
                <strong>직업:</strong> {{user.occupation}}
            </div>
        </div>
    </div>
</div>
{{>layouts/footer}}